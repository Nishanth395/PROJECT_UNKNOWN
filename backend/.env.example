# ==============================================================================
# PROJECT UNKNOWN - BACKEND ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to .env before running the backend.
# WARNING: NEVER commit actual API keys or secrets to version control.
# ==============================================================================

PROJECT_NAME=
ENVIRONMENT=
PORT=
HOST=
CORS_ORIGINS=

# Supabase Authentication & JWT Configuration
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# PostgreSQL Database (Direct connection URL with PostGIS)
DATABASE_URL=

# External Services (Placeholders for upcoming phases)
AI_API_KEY=
MAPS_API_KEY=

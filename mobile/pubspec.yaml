name: project_unknown_mobile
description: "Project Unknown - On-Demand Skilled Services Mobile Client"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  supabase_flutter: ^2.8.0
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
